# 挟みゲーム

## プロジェクト概要
2人対戦の盤上ゲーム。現在はローカル対戦のみ。
将来的にオンライン対戦（将棋ウォーズ型）とAI対戦に拡張する。
UIの作り込みを重視するため、演出を後から足せる構造を最優先する。

既存プロトタイプ `referencecurrent.html`（単一HTML）がルールとUIの参照実装。
挙動の正はこのファイル。

## 技術構成
- TypeScript + Vite（vanilla-ts、フレームワークなし）
- 盤面描画はDOM要素 + CSS transform。Canvasは使わない
- テストは vitest

## アーキテクチャ

依存の向きは `ui → app → core` の一方向。逆流は禁止。

```
src/
  core/       純粋関数のみ。DOM / 通信 / Date / Math.random に触れない
  app/        対局進行。core と ui の仲介
  ui/         描画・入力・演出
  main.ts     組み立てのみ
```

## 設計上の絶対ルール

1. **駒はIDで識別する。** 盤面は `Map<"x,y", PieceId>` で持ち、
   駒の実体は `Map<PieceId, Piece>` に置く。
   盤面に駒種を直接書かない（アニメーションで駒を追跡できなくなるため）

2. **`applyMove` は新状態と GameEvent[] の両方を返す。**
   盤面の差分比較では「何が起きたか」を復元できないため。
```ts
   applyMove(state, move):
     { state: GameState; events: GameEvent[] } | { error: MoveError }
```

3. **UI状態をGameStateに入れない。**
   選択中の駒・ハイライト・アニメ進行度は ui/ 側が持つ。
   判定基準は「オンライン時に相手へ送る必要があるか」

4. **GameState はイミュータブル。** 破壊的更新をしない

5. **`submitMove` は Promise を返す。**
   現在は同期処理でよいが、後で通信を挟むためシグネチャは非同期にする

6. **演出の変更はCSSに閉じる。** 移動は `transition: transform`、
   捕獲は CSS animation。JSでフレーム計算しない

## ゲームルール

駒を「取る」ゲームではない。挟んだ駒を**封じ**、相手の手を縛って詰ませるゲーム。

> 参照実装との差分: 「4つ以上つなげたら負け」は `referencecurrent.html` には無い追加ルール。
> それ以外は参照実装の挙動に一致する。

### 用語

| 語 | 意味 |
|---|---|
| 先手 / 後手 | プレイヤーA（黒）/ プレイヤーB（白） |
| 持ち駒 | まだ盤に出していない自分の駒。数は有限 |
| 打つ | 持ち駒を空きマスに置く |
| 封じ | 挟まれた状態。盤からは除かれない。次の自分の手番で最優先に動かす義務を負う |
| 壁 | 盤の外側。両プレイヤー共用の挟み手として働く |
| 塊 | 上下左右の隣接でつながった同じ側の駒の連結成分 |

### 盤と初期状態

- 正方形盤。既定 7×7（設定で 5 / 7 / 9）
- 開始時、盤上に駒はない
- 各プレイヤーの持ち駒は既定 8 個（設定で 6 / 8 / 12）
- 移動距離の上限 `range` は既定 1（設定で 1 / 2 / 3）
- 連結上限 `connectLimit` は 4（設定不可）
- 先手 A から交互に指す

盤サイズ・持ち駒数・移動距離は対局開始時に固定されるパラメータ。
GameState が保持し、core はハードコードしない。

### 手番にできること

封じ駒を持っていない場合、次のどちらか一つ。

1. **打つ** — 持ち駒 1 個を空きマスに置く
2. **動かす** — 盤上の自分の駒 1 個を上下左右に 1〜`range` マス動かす

移動は直線のみ。斜め不可。飛び越え不可で、経路上に駒があればその手前で止まる。

**封じ駒を 1 個以上持っている場合は「動かす」のみ。動かせるのは封じられている駒だけ。**
打つことはできない。封じ駒が複数あるときは、そのうち 1 個を動かせばよい
（動かさなかった駒は封じられたまま次の手番へ持ち越す）。

### 挟み（封じ）の成立

判定は**その手で動かした駒だけを起点に**行う。移動後の盤面で:

- **相手を封じる** — 動かした駒の上下左右に隣接するマスに敵駒があり、
  さらにその 1 つ先が自分の駒または壁（盤外）なら、その敵駒を封じる。
  4方向を独立に判定するので、1手で複数枚を同時に封じうる。
- **自分が封じられる** — 動かした駒自身が、縦軸または横軸で
  両側を敵駒または壁に挟まれていたら、その駒を封じる。
  ただし**両側とも壁**の場合（幅1の盤）は挟みとみなさない。

挟みの距離は常に隣接 1 マス。`range` を伸ばしても挟み判定の距離は変わらない。

離れた場所で結果的に成立した挟み（動かした駒が関与しない形）は判定しない。
すでに封じられている駒を重ねて封じても効果は変わらない。
動かした駒は、その移動によって自分の封じ状態が解除される
（解除された直後に再び自分が挟まれれば、また封じられる = 連鎖する）。

### 打つときの制限

打った結果として、

- 敵駒を挟む形になる打ち方
- 打った駒自身が挟まれる形になる打ち方

はどちらも**禁止**。挟みは必ず移動で成立させる。
持ち駒が 0、または封じ駒がある間も打てない。

### 連結の禁止（4つ以上つなげたら負け）

**自分の駒が上下左右の隣接で 4 個以上つながった塊を作ったら、その手を指した側の負け。**

- 連結は上下左右のみ。斜めはつながっているとみなさない
- 「塊」は連結成分。直線に4つ並ぶ必要はなく、L字やT字も 4 連結
- 打ちでも移動でも適用する
- 判定するのは**その手を指した側の駒だけ**。自分の手で相手の駒は動かないため、
  相手の塊が育つことはない
- 塊を作る手も**合法**。ただし指した瞬間に負ける。
  封じ駒の強制移動で逃げ場が塊しかない、という詰み方を成立させるための扱い
- 封じ状態は連結に関係しない。封じられた駒も塊の一部として数える

連結上限 `connectLimit` は 4 を既定とし、GameState が保持するパラメータとする。

### 決着

- 手を指した直後、指した側の駒が 4 個以上つながった → **指した側の負け**
- 手番が移った時点で、封じ駒があり、そのどれも動かせない → **その手番のプレイヤーの負け**
- 手番が移った時点で、封じ駒がなく、打つことも動かすこともできない → **その手番のプレイヤーの負け**
- 同一局面が 3 回現れた → **引き分け**

複数の条件が同時に成立した場合は上から順に優先する。
4 連結の自滅は、相手を封じ切った手でも負けになる。

同一局面の同一性は「盤上の各マスの駒の所有者 + 手番 + 両者の封じ駒の集合」で判定する。
駒IDそのものは含めない（同じ形なら同一局面）。

### 1手の処理順（applyMove の内部順序）

1. 合法手か検証する（不正なら `MoveError`）
2. 打つ／動かすを盤に反映する。動かした駒は自分の封じ集合から外す
3. 動かした駒による相手の封じを判定し、相手の封じ集合に加える
4. 動かした駒自身の被封じを判定し、自分の封じ集合に加える
5. 指した側の 4 連結を判定する。成立していればここで負けが確定し、
   以降の詰み・千日手の判定は行わない（手番は形式上相手に渡す）
6. 手番を相手に渡す
7. 相手の詰み（負け）を判定する
8. 同一局面の回数を数え、3回なら引き分け

### GameEvent の順序

`applyMove` が返す events は**描画順**に並べる。UI はこの順にアニメーションを流すので、
順序を変えると演出が崩れる。

```
moved → captured* → turnChanged → (gameEnded)
```

- `captured` は成立した挟みの数だけ並ぶ。相手を挟んだ分が先、自分が挟まれた分が最後
- `turnChanged` は決着した手でも必ず出る。順序を局面によって変えないため
- 封じの**解除**は独立したイベントにせず、`moved` の `released` で伝える。
  封じは必ず移動によって解けるため

## app 層の約束

UI は `GameSession` だけを見る。core を直接 import しない。

### 通知は2系統

1手が確定すると、まず `onEvent` がイベントの数だけ描画順に呼ばれ、
そのあと `onStateChange` が1回だけ呼ばれる。

```
onEvent(moved) → onEvent(captured)* → onEvent(turnChanged) → [onEvent(gameEnded)]
  → onStateChange(確定した局面)
```

- `onEvent` は「何が起きたか」。演出のための系統
- `onStateChange` は「局面が確定した」。描画を合わせるための系統

1系統にまとめない。イベントだけでは最終局面を取りこぼしたときに復帰できず、
局面だけでは何が起きたか復元できないため（設計ルール2と同じ理由）。

### seats

`GameClient.seats` は、そのクライアントが指せる側。
ローカル対戦は `["A", "B"]`、オンラインは自分の側だけ、AI対戦は人間側だけ。
UI の「自分の手番か」判定は `session.canAct` を見る。

### 待った

`GameClient` が局面の履歴を持ち、1手ずつ戻す。決着したあとでも戻せる
（負けた手を取り消すため）。

待ったは **GameEvent を流さない**。戻った局面には「何が起きたか」がないため、
`onStateChange` だけで伝える。UI は局面から描き直す。
駒は ID で追えるので、CSS の transition が元の位置へ滑って戻る。

`supportsUndo`（機能があるか＝ボタンを出すか）と
`canUndo`（いま押せるか）を分ける。オンライン対戦では前者が false。

### 棋譜

`GameSession` がイベントから組み立てる（`session.record`）。
局面を持たず、「何手目に誰が何をしたか」だけを並べる。

待ったで戻したぶんは消える。**手数がそのまま棋譜の長さ**になるので、
局面が確定するたびに `state.ply` まで切り詰めれば揃う。

表記は `core/notation.ts` に置く。盤の読み上げにも棋譜にも同じ関数を使うので、
書き方を変えたいときはそこだけ直す。

### 保存と再生

保存するのは「開始時の設定」と「指した手の並び」だけ（`ui/RecordStore.ts`、ブラウザに残す）。
局面は保存しない。同じ設定の開始局面に同じ順で流し込めば同じ対局になるため。

駒 ID は打った順に振られるので、指し直しても同じ駒に同じ ID が付く。
だから `core/rules.ts` の `moveOf` で棋譜の1手を着手に戻せば、そのまま再生できる。

再生は対局と同じ LocalClient / GameSession を作って1手ずつ指し直す。
進むのは submit、戻るのは待った。演出も音も棋譜も対局と同じ経路で出る。

### 二重送信

`GameSession.submit` は、前の着手が確定するまで次を `busy` で断る。
通信が入ると往復に時間がかかるため、連打で二重に送らせない。

## ui 層の約束

### 描くのは onStateChange、演出を集めるのは onEvent

1手の描画はこの2段構えで行う。

- `onEvent` … 一度きりの演出の材料を集めるだけ（打たれた駒・封じられた駒・挟みの線）
- `onStateChange` … 確定した局面で描き直し、集めた演出を流す

駒の移動そのものは演出として扱わない。局面を反映して `transform` を書き換えれば、
CSS の transition が勝手に動かす。盤の差分を取る必要がない。

### 盤の描画は BoardStage が持つ

盤・手番表示・棋譜をまとめ、対局の通知に合わせて描き直すところまでが `BoardStage`。
対局画面と再生画面はこれを共有し、違うのは「何で手が進むか」だけ。
入力も操作ボタンも BoardStage には持たせない。

### BoardView は局面と「飾り」を受け取るだけ

選択中の駒・行き先・打てるマス・つまめる駒は `BoardDecor` として外から渡す。
BoardView はルールを知らないので、盤の見た目を変えても合法手の判定に触らずに済む。

### 音

設計ルール6は「演出の変更は CSS に閉じる」だが、**音だけは CSS で作れない**。
代わりに `ui/Sound.ts` の1ファイルに閉じ込め、他のどこにも鳴らす処理を置かない。

- 音源のファイルは持たず、その場で合成する
- 鳴らす時刻は AudioContext の時計で予約する。setTimeout も requestAnimationFrame も使わない
- 挟みの音は移動が終わる頃、決着の音は幕が下りる頃に置く。
  この待ち時間は CSS 側の値と対になっているので、片方を変えたらもう片方も見る
- 自動再生の制限があるので、AudioContext は最初に鳴らすときに作る。
  必ず操作の流れの中から呼ばれること

### 指で触る画面

パソコンとスマートフォンの両方で遊ぶ。違いは hover の有無に出る。

- **hover の見た目は `@media (hover: hover)` で囲う。**
  囲わないと、触った指の跡が残ったままになる
- **押せるマスは `@media (hover: none)` で常に点を出す。**
  hover が来ない画面では、どこに打てるかを示すものが他にない。
  行き先の点より小さくして混ざらないようにする
- hover の代わりに `:active` で押した反応を返す
- 狭い画面では枠の余白を詰めて盤に幅を回す。7×7 で1マス 44px 以上を目安にする

### hidden 属性は display に負ける

`display` を指定した要素には、ブラウザ標準の `[hidden] { display: none }` が効かない。
要素ごとに書くと必ず漏れるので、theme.css で一度だけ強く指定してある。

### 一度きりのクラスは付け外しでは足りない

`just-sealed` のようなクラスは、外してすぐ付け直しても同じフレームにまとめられ、
アニメーションが走らない。付け直す前に一度レイアウトを読ませる
（`void el.offsetWidth`）。同じ駒が続けて封じられる場合に効く。

疑似要素で作る印（封じの朱い環など）はこの問題が起きない。
クラスが付いている間だけ存在するので、現れたときに一度だけ動く。

## 画面の流れ

```
スタート → 対局設定 → 対局
   ├ 棋譜一覧 → 再生
   └ 問題一覧 → 詰めはさみ
```

- **スタート** … 題字と現在の設定。対局へ進む。ルールを重ねて出せる
- **対局設定** … 盤の大きさ・移動できる距離・持ち駒の数。決めると対局が始まる
- **対局** … 盤と手番表示、待った／棋譜を保存／もう一局／対局設定／トップへ
- **棋譜一覧** … 保存した対局。再生に入るか、消す
- **再生** … 保存した棋譜を1手ずつなぞる
- **問題一覧 / 詰めはさみ** … 決まった手数で詰ませる問題を解く
- **作問** … 開発者だけの画面。URL に `?editor` を付けたときだけ出る

対局中の「対局設定」は幕に**重ねて**出す。やめれば対局はそのまま続く。
全画面に切り替えると対局を捨てることになるため。

「もう一局」は局面を戻すのではなく、**対局を作り直す**
（新しい LocalClient と GameSession）。待ったの履歴も持ち駒もまとめて初期化される。

## 詰めはさみ

「攻め方が指し始めて、相手がどう受けても N 手で必ず勝つ」局面を解く。

### 読み

`core/solve.ts` が素直な深さ制限つきの読みを持つ。
攻め方は1つでも勝ち筋があればよく、受け方はすべての手で負けるときだけ詰み。

**節点の数に上限を置き、読み切れなかったときは「勝てる」と言わない。**
分からないものを詰みだと言わないための保険で、答えは常に控えめに外れる。

問題は**持ち駒 0** で作る。打つ手が混ざると読む手が一気に増えるため。

### 問題データ

問題は `core/puzzles.ts` にソースとして置く。局面は `core/diagram.ts` の文字の並び。

`puzzles.test.ts` が問題ごとに**ちょうど N 手詰めであること**と
**最初の一手が1通りしかないこと**を毎回読み直して確かめる。
手で書き足したものが成り立っていなければテストが落ちるので、壊れた問題は公開されない。

### 受け方

`app/PuzzleClient.ts` が GameClient として受け方を指す。
中身は LocalClient をそのまま使い、攻め方の手のあとに続けて指すだけ。
局面・履歴・イベントの配り方は対局と同じ経路を通るので、UI もほぼ同じものが使える。

待ったは**攻め方の手番まで戻す**。受け方の応手だけ取り消された状態にはしない。

### 作問と公開

URL に `?editor` を付けると作問の画面が出る。どこからも繋がっていない。

1. 盤を押して駒を並べる（押すたびに 空き → 黒 → 白 → 黒の封じ → 白の封じ）
2. 「読む」で**ちょうど N 手詰め・答えが1通り**かを確かめる
3. 「書き出す」で `core/puzzles.ts` に貼れる形が出る
4. 貼って `npm test`（同じことをテストがもう一度確かめる）
5. `main` に push すると公開される

**問題を置く場所はソースだけ。** サーバも投稿の口も持たない。
公開の経路が push しかないので、テストを通らないものは外に出ない。

## 公開

GitHub Pages に出す。`main` に push すると、テストが通ったものだけが公開される
（`.github/workflows/pages.yml`）。

Pages はリポジトリ名のサブパスに置かれるので、`vite.config.ts` が `base` を付ける。
リポジトリ名はファイルに書かず、Actions が渡す `GITHUB_REPOSITORY` から決める。
手元では未設定なので `"/"` のまま動く。

## 進め方

- 指示された範囲だけを実装し、先回りして他のファイルを作らない
- core/ の変更時は必ずテストも書く
- 実装後、依存の向きが守られているか自己チェックする
